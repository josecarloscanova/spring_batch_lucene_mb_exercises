package org.nanotek.base;

/**
 * Same object as in "Predicate class" on commons collections except that does not impose 
 * that the  argument passing being done on method call.
 * 
 * @author josecanovamauger.
 *
 * @param <T>
 */
public interface Predicate<T> {

	public boolean evaluate();
	
}
