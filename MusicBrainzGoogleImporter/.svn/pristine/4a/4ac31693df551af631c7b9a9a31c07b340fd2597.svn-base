package tests;

import org.nanotek.Base;

public interface Mythical<T extends Base<?>> {
    default public String identifyMyself(T ability) {
        return "I am a mythical creature. " + ability.getId();
    }
}