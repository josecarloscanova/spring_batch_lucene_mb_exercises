package org.nanotek;

/**
 * 
 * Yeah yeah, we all know that, just more one interface to define a 
 * common method that might be used among other parts of the program. 
 * 
 * @author josecanovamauger.
 *
 * @param <T>
 */
public interface Conditionable<T> {

	public Conditionable<?> setCondition(T t);
	
}
